# 強化の候補（2026-09-24 時点の実測に基づく）

**2026-09 の国内計測を実際に回して分かったことを集めた。** 設計の意図と実装の
間、実装と実行環境の間に差があり、その差は単体の検査では出ない。優先順位を
付けてある。

（OWNER-TASKS.md は以前「実装側の残件は無い」と書いていた。この文書がその
反例なので、そちらの記述も直した。）

数字はすべて実行ログか manifest から取った（国内の run 35939280226 と
35963788831、2026-09 の米国 50 社の動作確認）。推定には推定と書いた。

---

## 優先度の考え方

1. **数字の正しさに関わるもの**が先。出ている数字が母集団を代表していないなら、
   速くしても意味が無い
2. 次に**初回の全件計測を完走させるもの**。1回通らないと辞書も判断材料も出ない
3. 検査・文書の整合はその後。ただし「宣言を見ていて実装を見ていない検査」は
   このリポジトリが何度も踏んだ形なので、見つけたものは書く

---

## 1. 国内上場企業の 47.7% に起点ドメインが無い ── **a・b・c は対応済み**

> 法人番号（P3225）で Wikidata と突合し、**空欄だけ**を埋めるようにした
> （`jp_houjin_identity.rq`、全 jp-* 母集団）。gBizINFO の後に置いてあり、
> 政府の一次情報は上書きしない。出典に Wikidata を足した。
>
> **被覆率はまだ分からない。** この環境から WDQS が引けないので、次の月次
> 計測の manifest（`breakdown.wikidata_identity.filled`）で確かめること。
> 1件も埋まらなければ `WIKIDATA_FILLED_NOTHING` が出る。
>
> **b・c も対応済み。** gold に `entities_with_domains` を足し、総括の
> 「企業 N 社」の横と方法論（日英）に差を出した。doctor は設定が済んだ段階で
> 欠けの大きい月を次の一手に出す（閾値 25%）。
>
> **d（米国をティッカーで突合）は未着手。**


```
[p1_population] input=11387 success=3818
  ⚠ ACCEPTANCE_MISSING_RATE: official_url の欠損率 47.7% が閾値 10.0% を超えている
```

P1 は 3,818 社を確定したが、**official_url が取れたのは約 52%**（起点ドメインの
重複排除後で 1,992 件）。gBizINFO の `company_url` は半分の企業で空である。

起点が無い企業は P2 で候補ゼロになり、P3 以降に一度も現れない。**分母から
黙って消える。** P2 の進捗（`候補=1238 / 2177 社処理`）が示す 1 社あたり
0.57 ドメインという数字は、これが原因である（受け入れ基準は中央値 1〜5）。

現状のサイトは「企業 3,818 社」と出しながら、実際に測れるのはその半分である。
**原則5（取れなかったと無かったを区別する）が、母集団の入口で破れている。**

### 直し方

**a. Wikidata（CC0）を国内の補完経路にする。** 米国側では既に
`configs/populations/_sparql/us_cik_identity.rq` が CIK → P856（公式サイト）を
1クエリで引いている。Wikidata には **P3225（法人番号）** と **P5090（EDINET
コード）** があり、同じ形で `法人番号 → P856` が引ける。

  - `fetch_cik_identity()` を鍵の種類に依存しない形に一般化する
    （`src/mailauth/p1_population/wikidata.py:170`）
  - `jp-all-listed.yaml` の `enrich` に `wikidata_identity` を足し、
    `wikidata_identity_query` に `jp_houjin_identity.rq` を指す
  - 出典に「Source: Wikidata (CC0)」が自動で乗ることは PR #45 で保証済み
  - **鍵が要らない。** doctor の `OFFICIAL_URL_SOURCES` では既に
    `wikidata_identity: None`（認証不要）として扱われている

被覆率がどこまで上がるかは**まだ測れていない。** Wikidata の国内上場企業の
網羅は米国の小型株より良いはずだが、推定である。

**この開発環境からは測れない。** WDQS も wikidata.org 本体も 403 を返す
（`Please respect our robot policy`）。User-Agent の問題ではなく
**出口 IP が弾かれている** ── 素の項目取得でも同じく 403 になる。
Actions からは別の IP なので通る（米国側の実測 4,199行 / website 3,975件は
そこで取ったもの）。**測るなら Actions で回すこと。**

**b. 起点の無い企業の数を gold と公開サイトに出す。** `StatsOverall` には
`total_entities` / `total_domains` / `observed_domains` があるが、
**「候補を1つも持たない企業数」が無い。** 読み手は 3,818 社を測ったと読む。
`entities_without_domains`（または `entities_with_domains`）を gold に足し、
方法論ページに「起点が取れなかった企業は率に入っていない」と書く。

**c. doctor に見せる。** `doctor.py` は `observed_domains == 0` のときだけ
official_url の欠損を疑う（`doctor.py:441`）。47.7% は `status=success` で通る。
P1 manifest の `ACCEPTANCE_MISSING_RATE` を読んで次の一手に出すこと。

**d. 米国も同じ問題がより深い。** `us-all-listed.yaml` は official_url の欠損を
80% まで許容している（Wikidata の CIK 付き項目が 25.6%）。50社の動作確認では
候補ゼロが 40% だった。**ティッカー（P249）で突合する**と上がる可能性がある ──
SEC の `company_tickers.json` は CIK ↔ ティッカーを持ち、Wikidata は CIK より
ティッカーの付与率が高い。これも実測が要る。

---

## 2. 時間切れで残るのは CT のキャッシュだけ ── **対応済み（工程の飛ばしを除く）**

> P1 の補完（gBizINFO / 法人番号 / SEC / EDINET）も持ち越すようにした。
> 月で区切り、CT とは別の鍵・別の場所にしてある（`data/cache` をまとめて
> 指すと、復元の順番で新しい CT に古いものを被せる）。
>
> **b・c も対応済み。** `data/runs/<run_id>` を P3 の直後と P4 の直後に
> 確保し（`if: always()`）、次の実行の先頭で復元する。P4 は `--resume` で
> 測り終えたドメインを飛ばす。bronze は1ドメインごとにフレームを閉じるので、
> **殺された時点で「最後まで書けたドメイン」が確定している。**
>
> **工程の飛ばし（済んだ P1〜P3 を再実行しない）は未着手。** 出力は復元
> されるが、工程そのものは毎回走る（原則6 なので結果は同じ）。
>
> 以下は直す前の記録。


`monthly.yml` で `if: always()` が付いているのは「CT ログのキャッシュを保存」
（step 10）だけである。P1 の出力、P3 の出力、P4 の bronze はジョブが
時間切れになると**全部消える。** 退避（R2）もコミットも最後の工程にある。

2026-09 の 2 回目の実行（run 35963788831）で実際に起きたこと：

```
06:18  CT キャッシュ復元    987 件が当たった（restore-keys の前方一致は効く）
06:30  P1                  12 分 47 秒
10:52  P2                  4 時間 21 分（新規 1,005 件、失敗 188 = 18.7%）
11:16  P3                  23 分 26 秒（2,158 ドメイン、直列）
11:47  P4                  31 分で時間切れ。245 / 2,153 ドメイン（11.4%）
       P1〜P4 の成果は CT キャッシュ以外すべて消えた
```

**P4 は 1 ドメイン 7.7 秒**（14,249 クエリ / 245 ドメイン = 58 クエリ/ドメイン、
実効 7.5 qps）。2,153 ドメインで **4 時間 36 分**かかる。

3 回目を回しても、P1 13 分 + P2（失敗 188 件の取り直し）+ P3 23 分 + P4 4 時間
36 分で **5 時間 30 分の上限をまた超える。** CT が全部キャッシュに乗っても
完走しない。**P4 を速くするか途中から再開できるようにするまで、3 回目を
回す意味が薄い。** これは「米国を足したとき」の話ではなく、国内 1 母集団の
初回計測がいま止まっている理由である。

### 直し方（安い順）

**a. gBizINFO と法人番号のキャッシュも Actions のキャッシュに乗せる。**
P1 の 773 秒はほぼ全部 gBizINFO の 5 qps × 3,818 社である
（`enrich.py:58`、キャッシュは `data/cache/gbizinfo/`）。月をまたいでも
法人番号は変わらないので、キャッシュの鍵は月で区切らなくてよい。
**P1 が 13 分から 1 分になる。** 変更は monthly.yml だけ。

**b. 工程ごとに `data/runs/<run_id>/` を保存する。** P1・P3 の出力と bronze を
`actions/cache/save` で `if: always()` 付きで工程直後に保存し、次の実行の先頭で
復元する。P1〜P3 は入力が同じなら出力も同じ（原則6）なので、**復元できたら
その工程を飛ばす**判断が要る。manifest の有無で判定できる。

**c. P4 を途中から再開できるようにする。** bronze は追記のみなので、同じ run に
既存パートがあれば、そこに含まれるドメインを飛ばす `--resume` を足す。
いまは `BRONZE_APPENDED_TO_EXISTING` の警告を出して**同じドメインを二重に
測る**（`p4_measure/runner.py:310`）。再開時の manifest は「今回測った件数」と
「前回分を合わせた件数」を分けて記録すること（原則4）。

---

## 3. P4 は直列で、設定の qps に届いていない ── **対応済み**

> 応答待ちだけを重ねるようにした（`rate.concurrency` / `resolver.concurrency`、
> 既定 8）。P3 も同じ形にしてある。投げる間隔は全スレッドで共有しているので、
> **権威DNSから見た本数は直列のときと変わらない。** 以下は直す前の記録。

`configs/measure.yaml` は `qps: 80` だが、P4 は 1 本ずつ問い合わせて応答を
待つ（`p4_measure/runner.py:208-302`）。実測：

```
米国 50 社（階層 C が多い）  2,790 クエリ / 134.6 秒 = 20.7 qps、1 ドメイン 1.4 秒
国内 全件（階層 A が多い）  14,249 クエリ / 1,886 秒 =  7.5 qps、1 ドメイン 7.7 秒
```

国内は DKIM セレクタ約 50 本が乗るので 1 ドメイン 58 クエリになり、1 クエリ
約 130 ms の往復がそのまま積み上がる。**上流（8.8.8.8）への往復時間で
律速している**ので、qps の設定値は何も制限していない。

DESIGN.md P4 の試算「582,000 クエリを 80 QPS で約 2 時間」は、この実装では
**7.8〜21 時間**になる。Sprint 3 の完了条件「30,000 ドメインで 3 時間以内」は
満たしていない。README は P4 を「実装済」と書いている。**国内 2,153 ドメイン
でも 4 時間 36 分かかり、上の 2 で書いたとおり初回計測が完走しない。**

P3 も同じ形で直列に 3 本ずつ引いており、2,158 ドメインで 23 分かかった。
P4 を直すときに同じ手当てを入れる。

### 直し方

ctlog.py と同じ形で、**投げる間隔は全スレッドで共有し、応答待ちだけを重ねる。**
ただし `DnsResolver` はスレッド安全に作られていない：

  - `_throttle()` にロックが無い（`resolver.py:111`）。ctlog.py の
    「ロックを保ったまま眠る」を移植する
  - `_cache` は素の dict（`resolver.py:99`）。読み書きにロックが要る
  - `stats` の加算も競合する
  - `BronzeWriter` は単一の書き手を前提にしている（`bronze.py`）。worker からは
    書かせず、`pool.map` の入力順で主スレッドが書く（ctlog.py の `prefetch()` と
    同じ構造）。**bronze の並び順が実行ごとに変わらない**ことが原則6 に関わる

`shuffle_domains`（同一権威への連続クエリ回避）は並行にしても効く。
qps の上限そのものは変えない。**上限に届いていなかっただけ**である。

---

## 4. crt.sh の失敗が実時間の大半を食っている ── **a は対応済み**

> 応答時間の分布（p50 / p90 / p99 / max / mean）と、失敗の内訳
> （timeout / http / transport / decode）、投げ直した件数を P2 の
> manifest（`breakdown.ct_response`）に入れた。**b と c はこの数字を
> 見てから判断する** ── 材料が無いまま調整すると、次の実行も5時間かけて
> 同じことを学ぶ。

run 35939280226 の P2：

```
1,165 件処理 / 179 件失敗（15.4%）
1 件あたり 16.3 秒（同時 4 本 → 応答 1 本あたり ≈ 65 秒）
```

`timeout_sec: 60` に対して平均応答が 65 秒というのは、**多くが時間切れで
失敗している**ことを意味する。失敗 1 件の最悪は 60 + 2 + 60 + 4 + 60 = 186 秒で、
4 本の枠を 1 つ占有し続ける。179 件 × 186 秒 ÷ 4 ≈ **2.3 時間** ── 5 時間 15 分の
うち 4 割が失敗の待ち時間だった可能性がある（上限値からの推定。実測ではない）。

手元では 1 件 2.4〜6.4 秒で、ランナーでは 16.3 秒。**crt.sh は GitHub Actions
の IP 帯に対して遅い。** これは相手の事情なので、こちらで変えられるのは
「何を頼むか」と「どこまで待つか」である。

2 回目（新規 1,005 件）では失敗が **18.7%** に上がった。しかも時間とともに
増えている：

```
経過  15 分   失敗   3 件（新規  71 件処理）
経過  42 分   失敗  15 件（新規 192 件）
経過 1 時間 43 分  失敗  59 件（新規 433 件）
経過 3 時間 05 分  失敗 127 件（新規 732 件）
経過 4 時間 21 分  失敗 188 件（新規 1,005 件）
```

序盤は 4%、終盤は 25% 前後。**同じ IP から数時間続けて叩くほど遅くなって
いる**ように見える（推定。相手側の制限か、たまたま重いドメインが後ろに
偏ったかは、この数字だけでは分からない）。下の a で応答時間を記録すれば
判別できる。もし前者なら、同時本数を増やす方向は逆効果で、**塊ごとに
休む**方が失敗が減る可能性がある。

#### run 9（36025105807）は、その「時間とともに増える」を再現しなかった

5 時間 16 分で打ち切り。1,861 / 3,191 まで進み、うち**キャッシュ命中 1,139・
実際に取りにいったもの 722・失敗 419**。

```
経過      処理   cache   取得   失敗   失敗率   区間の取得/分   区間の失敗率
0.47h     153      88     65     42   64.6%         2.29         64%
1.37h     565     359    206    109   52.9%         2.67         49%
2.34h     916     577    339    187   55.2%         2.17         63%
3.65h    1349     833    516    289   56.0%         2.13         66%
4.56h    1639     999    640    356   55.6%         2.23         54%
5.05h    1793    1093    700    398   56.9%         2.06         70%
```

**増えていない。最初の 30 分から 55% 前後で平ら**で、取得の速さ（2.1〜2.7
件/分）も 5 時間を通して平らである。run 8 で見えた 4%→25% の上昇は、
**run 9 では再現しなかった。**

かわりに別の筋が立つ（**推察**）。キャッシュに入るのは**成功したものだけ**
なので、実行を重ねるほど**残りは「前回失敗したドメイン」に濃縮される。**
run 8 が時間切れにした重いドメインが、run 9 では最初から混ざっている ──
だから序盤から 55% で、下がりもしない。これが正しければ

  - 失敗は**相手の制限ではなくドメインの重さ**で決まる
  - 同時本数を増やしても失敗率は変わらない（枠を空けるだけ効く）
  - **b（`exclude=expired`）が本命**になる。軽くすれば時間切れが減る
  - 実行を重ねても収束が遅い。**残る母集団が毎回難しくなる**

`breakdown.ct_response` の `errors_by_kind` で判別できる。**timeout に偏って
いれば重さ、http / transport に散っていれば相手側の制限**である。

#### 1 件 N 秒は crt.sh の速さではない

進捗行の「1 件 10.2 秒」は**キャッシュ命中を含めた処理件数**で割った値で、
crt.sh の応答時間ではない。実際に取りにいった 722 件で割ると**1 取得あたり
26.3 秒**、同時 4 本なので**応答 1 本あたり最大 105 秒**（枠が常に埋まって
いた場合の上限）。

同じ行の「失敗=419」も分母が違う。**失敗はキャッシュ命中では起きない**ので
分母は 722 であり、**58%** ── 2 件に 1 件以上が失敗している。手近にある
1,861 で割ると 22.5% に見える。**実際にそう読み違えた。** 進捗行に `取得=`
を並べて、1 行の中で割れるようにした。

なお `qps: 0.5` に対する実際の投げ方は **722 / 18,960 秒 = 0.038 req/s** で、
**上限の 8%** しか使っていない。同時本数は qps とは別のつまみなので、
礼儀として置いた上限を守ったまま増やす余地はある ── ただし上の判別が
先である。

### 直し方（判断が要る）

**a. まず測る。** ~~manifest に応答時間の分布を入れる~~ **済**。
`breakdown.ct_response` に p50 / p90 / p99 / max / mean、`errors_by_kind`
（timeout / http / transport / decode）、`retried`、設定していた `timeout_sec`
が出る。**キャッシュに当たった分は混ぜていない**（crt.sh の速さではないため）。

**b. `exclude=expired` を試す。** crt.sh の識別子検索は期限切れの証明書も
返すので、大企業ほど行数が膨れて遅くなる。期限切れを除くと応答が軽くなり、
時間切れが減る見込みがある。**意味が変わる**（期限切れ証明書にしか無い
ドメインが候補から落ちる）が、そのドメインは P3 の一次実証でどのみち落ちる
可能性が高い。実測して比較すること。

**c. 時間切れの扱い。** 「60 秒 × 3 回」より「120 秒 × 1 回」の方が、遅いだけの
相手には成功率が上がる。

**枠の占有時間は「同じ」ではない。** 最初にそう書いたが、数えると違う ──
いまの設定（timeout 60・retries 2）の最悪は 60 + 2 + 60 + 4 + 60 = **186 秒**で、
120 秒 × 1 回より **66 秒長い。** 待ち方を変える方が、実は枠も空く。

これも a の数字を見てから。**run 9 は a より前のコードで回っているので、
切り分けられるのは run 10 以降である。**

ただし、**延期の理由だった「時間とともに悪化するなら逆効果」は run 9 では
起きていない**（上）。悪化が前提の慎重さはもう要らないかもしれないが、
それは `errors_by_kind` を見てから言うことである。

**d. 文書の数字を現実に合わせる。** `ctlog.py` の冒頭と `candidates.yaml` は
「同時 4 本で 1 件 2.4 秒、8 本でも変わらない、qps の下限に届いている」と
書いている。**ランナーでは 16.3 秒で、下限（2.0 秒）の 8 倍である。** 手元の
測定は手元の条件で正しかったが、そのまま読むと「もう速くならない」と誤読する。
両方の数字を条件付きで残すこと。

---

## 5. 検査が宣言を見ていて、実装を見ていない ── **対応済み**

このリポジトリが繰り返し踏んだ形（PR #45 の attribution も同じだった）。

> **a・b も対応済み。** 検査は「同じ名前を2回引いても問い合わせは1本」を
> 見るようにした。`cache:` の2キーは消し（切れるようにしていないので設定では
> ない）、`resolver.mode` と `extras.https_fetch` は `_status: planned` を
>添えた。`tiers:` は**実際に読むようにした** ── 階層ごとの queries で投げる
> 種類を絞り、applies_to_confidence で階層の割り当てを決める。
>
> **新しく読まれないキーが増えたら落ちる検査**を足した。それが `tiers:` と
> `applies_to_confidence` を見つけた（この文書に挙げていなかった2件）。
>
> 以下は直す前の記録。

**a. `test_spf_includes_and_mx_hosts_are_cached`**（`tests/test_acceptance.py:226`）
は `measure.yaml` に `mx_host_resolution: true` と**書いてあること**を検査している。
このキーを読むコードは存在しない（`grep -r mx_host_resolution src` → 0 件）。
実際の負荷回避は `DnsResolver` の in-memory キャッシュが担っていて、それは
動いている。**検査を「同じ名前を 2 回引いても問い合わせは 1 本」に直す**か、
読まれないキーを消す。

**b. 読まれていない設定キー**（`configs/measure.yaml`）：

| キー | src での参照 |
|---|---|
| `cache.mx_host_resolution` | 0 |
| `resolver.mode: local_unbound` | 0 |
| `extras.https_fetch` | 0 |

原則7（設定はコード外に）は「設定がコードを変える」ことが前提である。
変わらない設定は、読む人に無い機能があると思わせる。実装するか、
「未実装」と注記するか、消す。

**c. ~~公開サイトの挙動を検査していない。~~ 対応済み。** CI の `site` ジョブが
ビルド後に実際の画面を開き、17項目を確かめる（`site/scripts/check-browser.mjs`）。
**元の不具合を再現させて、落ちることを確認した** ── 2つの手当てを両方外すと
`observablehq-search-results intercepts pointer events` で落ちる。

この検査を書いたときに**当たり判定が 23px しか無い**ことが分かった
（WCAG 2.5.8 は 24x24）。`min-height` で直した。

以下は直す前の記録。 iPhone で左下の切り替えが押せなかった
不具合は、要素の存在・属性・CSS 変数を全部確認したうえで見逃した。
**押していなかったから**である。いまの検査（`test_compliance.py:1057`）は
CSS の文字列を見ている。`z-index` を消せば落ちるが、Framework が構造を
変えたら文字列は残ったまま挙動だけ壊れる。

CI の `site` ジョブに Playwright を足し、**実際にタップする**：
iPhone 13 相当で開き、ダーク/ライト・JPN/ENG を押して属性と遷移を確認、
Alt-K で検索欄に合う、横はみ出し 0、コンソールエラー 0。開発中に使った
スクリプト（22 項目）がそのまま土台になる。1 分程度の追加で済む。

---

## 6. P4 の manifest は入力と成功の単位が違う ── **対応済み**

> `counts` をドメイン単位に揃えた。`success + failed + skipped == input` が
> 成り立つ。一部だけ引けたドメインは成功に含めつつ
> `breakdown.domains.partially_observed` に分けて残す（**半分しか見ていない
> ことが「成功」に埋もれない**）。クエリ数は `breakdown.queries_total`。
>
> **1ドメインの全滅が130件の失敗に見えていた**（クエリごとに数えていたため）。
> いまは1件である。
>
> 以下は直す前の記録。

```
| p4_measure DNS計測 | partial | 96 | 2773 | 17 | 0 |
```

入力はドメイン数、成功はクエリ数（`runner.py:329`）。工程レポートの
「工程間の件数」表は p3→p4 で 96→96、p4→p5 で 2773→2790 を並べるが、
後者はクエリとレコードの比較で、**表の意図（どこで何件落ちたか）が読めない。**
`domains_measured` を足し、`counts` はドメイン単位に揃える。クエリ数は
`breakdown.queries_total` に既にある。

---

## 7. 規模に対する前提

いまの国内 1 母集団では問題にならないが、DESIGN.md の 30,000 ドメインでは
効いてくるもの。

- **P5 が bronze を全部メモリに載せる**（`p5_parse/runner.py:335`）。
  582,000 レコードの dict は数 GB になる。ファイル単位・ドメイン単位に流す
- **P5 も直列で DNS を引く**（Tree Walk と rua 宛先検証。`cli.py:252`）。
  月次では `--no-dns` を付けていないので**動いている**（方法論ページの記述は
  正しい）。ただし P4 と同じ理由で遅い
- 6 時間の壁は P2（CT）・P4（DNS）・P5（DNS）の合計で見る必要がある

---

## 8. 文書と現実の差 ── **対応済み**

**すべて対応済み。** 直した内容は次のとおり。

| 場所 | 書いてあったこと | どう直したか |
|---|---|---|
| OWNER-TASKS.md 冒頭 | 実装側の残件は無い | BACKLOG が実装側の作業であると明記 |
| DESIGN.md P4 | 80 QPS で約 2 時間 | **固定の時間を書かない。** 達成 qps は実行ごとに変わるので `runs/<月>.md` を正本にした |
| DESIGN.md §1.3 | 約1.9時間 / 80 QPS | 目標3時間以内とし、実測は `runs/` を見る形に |
| DESIGN.md Sprint 3 | 30,000 ドメインで 3 時間以内 | 目標は残し、**この規模の実測がまだ無い**ことを添えた |
| DESIGN.md P4 | zdns が主力 | `dnspython` が既定、zdns は任意 |
| ctlog.py / candidates.yaml | 4 本で qps の下限に届いている | **測る場所で答えが変わる。** 開発機とランナーの両方を条件付きで残した |
| OWNER-TASKS.md 1 | 3,830 社 ÷ 0.5 qps = 最低 2.1 時間 | qps だけから出した下限であると明記し、実測（1件16.3秒・4時間21分）を並べた |

数字は実行のたびに変わるので、文書に埋めるなら「いつ・どこで測ったか」を
添える。`runs/` の工程レポートが正本で、文書はそれを参照する形が続けやすい。

---

## 9. 小さいもの ── **Google Fonts の同梱を除き対応済み**

- ~~`site/static/_headers` に `Strict-Transport-Security` と
  `Content-Security-Policy` が無い~~ **ヘッダは対応済み。**
  CSP は「いま読んでいる先をそのまま書いたもの」で、絞るためではなく
  **知らないうちに増えたら止まるようにする**ために置いた。ブラウザ検査が
  本番と同じヘッダを付けて配り、違反が出たら落ちる。
  HSTS は2年・サブドメイン込み、preload は付けていない（外すのに時間が
  かかるので運営者が判断する）
- **Google Fonts の件は開いたまま。** ヘッダを足しても訪問者の IP が
  Google に渡ることは変わらない。閉じるにはフォントを同梱するしかない。
  **読み先は思っていたより多い** ── 設定に書いた3つ（Chakra Petch /
  Noto Sans JP / JetBrains Mono）に加えて、**Observable Framework 自身が
  Source Serif 4 を読んでいる**（ブラウザ検査の CSP 違反ログで分かった）。
  Noto Sans JP は日本語なので同梱すると重い。やるなら字形の絞り込みと
  セットで、**別の作業として見積もること**
- ~~P2 の受け入れ基準（中央値 1〜5）は起点の被覆が前提になっている~~
  **対応済み。** 候補ゼロの企業が4分の1を超えていて中央値が下振れして
  いるときは、**その社数を文面に出す。** 症状（中央値が低い）だけを毎月
  出しても、読み手は次に何をすればよいか分からない。上振れのときや、
  候補ゼロが少ないときは原因として挙げない
- ~~5時間半の工程の途中経過が、終わるまで読めない~~ **対応済み。** GitHub は
  実行中のジョブのログを返さない（404）ので、P1 が終わっていても分母が
  どれだけ埋まったか分からなかった。manifest を成果物に上げるようにした
  （成果物は実行中でも取れる）。**ステップ要約だけでは足りない** ── あちらは
  ページを見る人には見えるが API からは読めない。P2〜P4 にも同じ手が要るかは、
  次の実行で判断する
- ~~`ENRICH_SKIPPED_*` が「引いていない」と「引いて失敗した」の両方に付く~~
  **対応済み。** 引いて失敗した経路を `ENRICH_FAILED_*` に分けた。
  **混ざっていたのは Wikidata の2か所だけ**で、gBizINFO と法人番号は
  `skipped_reason`（鍵が無い）と `error`（引いて失敗）を元から分けていた。
  ただし **`error` は breakdown に入るだけで警告が出ていなかった** ──
  500社ぶん失敗しても `status=success` で通り、被覆率だけが静かに落ちる。
  そちらも `ENRICH_FAILED_*` を出すようにした

---

## 手を付ける順（提案）

| 順 | 項目 | 変わること | 大きさ |
|---|---|---|---|
| ~~1~~ | ~~3 P4（と P3）の並行化~~ **済** | P4 4 時間 36 分 → 約 35 分の見込み（qps 80 が上限として効きはじめる手前） | resolver + P4 + P3 + 検査 |
| ~~2~~ | ~~2a gBizINFO のキャッシュを Actions に乗せる~~ **済** | 同じ月の流し直しで P1 13 分 → 1 分 | workflow |
| ~~3~~ | ~~1a Wikidata 法人番号 → 公式サイト~~ **済** | 分母が半分から全体に近づく（**効果は次の計測で判明**） | P1 + SPARQL 1 本 + 検査 |
| ~~4~~ | ~~1b/1c 起点の無い企業数を gold・サイト・doctor に出す~~ **済** | 読み手が分母を知る | P7 / P8 / site |
| ~~5~~ | ~~2b/2c 工程の保存と P4 の再開~~ **済** | 時間切れで成果が消えない | workflow + P4 |
| ~~6~~ | ~~4a crt.sh の応答分布を manifest に~~ **済** | 次の判断が数字でできる | ctlog + P2 |
| ~~7~~ | ~~5c Playwright を CI に~~ **済** | 「押せない」を機械が捕まえる | CI + site |
| ~~8~~ | ~~5a/5b 読まれない設定キーと宣言検査~~ **済** | 検査が実装を見る | 小 |
| ~~9~~ | ~~6・8・9~~ **済** | 整合 | 小 |

**1 が無いと 3 回目も完走しない**（2 の実測）。1〜3 を入れてから 3 回目を回すと、
その数字がそのまま初月の gold になる。**逆に、1a を入れずに gold を確定すると、
翌月に分母が倍になって前月差分が意味を失う。** 母集団を変えて計測し直すと
月次の比較ができなくなる、という `us-all-listed.yaml` の注記と同じ理屈である。

3 回目を回すときの見込み（1〜3 を入れた場合）：P1 1 分 + P2 約 15 分（失敗 188 件の
取り直し）+ P3 数分 + P4 約 35 分 + P5〜P8 数分 ≈ **1 時間**。
