# 作業領域。bronze / silver は Git に入れない（DESIGN.md 第4章）
# 履歴肥大化の回避のため。退避先は Cloudflare R2。
data/runs/
data/cache/

# gold/ は集計結果のみコミットする（.gitignore しない）

.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
dist/
build/

.env
.env.local

node_modules/
console/frontend/dist/
console/frontend/tsconfig.tsbuildinfo
site/dist/
# Observable Framework のキャッシュは src/ の下に出る
site/**/.observablehq/
# P8 が gold から書き出す。gold を正本とし、生成物は入れない
site/src/data/*.json
site/src/data/*.csv

.DS_Store
