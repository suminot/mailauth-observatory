[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mailauth-observatory"
version = "0.1.0"
description = "Monthly measurement of email authentication (SPF/DKIM/DMARC/BIMI/MTA-STS/TLS-RPT/DNSSEC) for listed companies in Japan and the US"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
authors = [{name = "Takahiro Sumino"}]

dependencies = [
    "pydantic>=2.6",
    "typer>=0.12",
    "pandas>=2.1",
    "pyarrow>=15.0",
    "pyyaml>=6.0",
    "httpx>=0.27",
    # P2/P3 の軽量な一次実証に使う。P4 の本格計測は zdns を主力にする
    "dnspython>=2.6",
    # bronze の JSON Lines を固める
    "zstandard>=0.22",
    # PSL 解決。Tree Walk との差分検出に使う
    "publicsuffixlist>=0.10",
]

[project.optional-dependencies]
console = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "duckdb>=0.10",
]
# bronze / silver の R2 退避。S3 互換なので boto3 で話す。
# 月次実行だけで使うので本体の依存にはしない
offload = [
    "boto3>=1.34",
]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]

[project.scripts]
mailauth = "mailauth.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# "." はコンソール（console パッケージ）を import するために必要。
# console は配布パッケージではなくローカル専用ツールなので src/ の下に置いていない。
# `python -m pytest` は cwd を sys.path に入れるが `pytest` は入れないため、
# ここで明示しないと CI（pytest 直叩き）だけで落ちる。
pythonpath = ["src", "."]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
