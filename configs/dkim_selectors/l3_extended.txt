# DKIM セレクタ辞書 L3（拡張）— 現在は無効
#
# 状態: 開発予定（planned）
#
# 想定していた出典は Tatang 辞書（3,498語）だが、これは GPL-3.0 である。
# 本リポジトリは Apache-2.0 で公開するため、辞書データを取り込むことで
# コピーレフトがどこまで波及するかの法務確認が済むまで、L3 は無効とする。
#
#   configs/measure.yaml:
#     dkim.layers      -> [l1_core, l2_provider]   （l3_extended を含めない）
#     dkim.l3_on_miss  -> false
#     dkim.l3_status   -> planned
#
# 運用コンソールの画面5では L3 に「開発予定」バッジを出し、
# GPL-3.0 の法務確認待ちである旨をツールチップで示すこと（DESIGN.md 7.2）。
#
# 法務確認が済んだら、辞書本体はこのファイルに取り込まず、
# 別リポジトリ（GPL-3.0）に置いて実行時に取得する構成を検討すること。
# そのほうがライセンスの境界が明確になる。
#
# このファイルは意図的に空である。
